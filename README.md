# Caliber Labs

Next.js app lives in **`caliber-labs/`**.

**Vercel:** In Project Settings → General → **Root Directory**, set to `caliber-labs` (then Save and redeploy). Otherwise you get "Code not found" because the repo root has no `package.json`.
