# Caliber Peptides

Next.js app lives in **`caliber-peptides/`**.

**Vercel:** In Project Settings → General → **Root Directory**, set to `caliber-peptides` (then Save and redeploy). Otherwise you get "Code not found" because the repo root has no `package.json`.
